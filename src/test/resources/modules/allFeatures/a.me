
###
function that creates an array of fibonacci numbers
###
fibonacci(n) = {
    if (n == 0) {
        return [0];
    } else if (n == 1) {
        return [0, 1];
    } else {
        fib = fibonacci(n - 1);
        fib[n] = fib[n - 1] + fib[n - 2];
        return fib;
    }
}
